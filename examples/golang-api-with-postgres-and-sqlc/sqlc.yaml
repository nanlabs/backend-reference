version: "2"
sql:
  - engine: "postgresql"
    queries: "query.sql"
    schema: "schema.sql"
    gen:
      go:
        package: "users"
        out: "users"
        sql_package: "pgx/v5" # you can use "database/sql" or "pgx/v5"